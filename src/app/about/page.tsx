import Loader from "./Loading";
import Loading from "./Loading";

const About = () => {
  return (
    <div >
     <Loader/>
      this is about page
    </div>
  );
};

export default About;
